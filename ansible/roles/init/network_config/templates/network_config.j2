network:
  version: 2
  renderer: networkd
  ethernets:
    ens18: # network adapter
      dhcp4: no
      addresses: [{{ static_ip }}/24]
      routes:
        - to: default
          via: {{ gateway }}
      nameservers:
          addresses: [1.1.1.1,8.8.8.8] # dns resolution
  